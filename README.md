## Quick Start
Follow the steps below to run the project locally:

1. In the root directory of the project, install dependencies:
   ```bash
   npm install
   
2. Run the project on the local development server:
   ```bash
     ng serve
3. Build the project for production:
   ```bash
     ng build
